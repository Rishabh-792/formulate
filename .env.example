# Formulate runs fully offline with the built-in mock interpreter/explainer.
# Set all four values below to switch MODE to "azure" (auto-detected).

AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-10-21

# Optional overrides
# FORMULATE_LOG_LEVEL=INFO
# FORMULATE_SOLVER=appsi_highs
