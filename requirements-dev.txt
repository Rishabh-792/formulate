-r requirements.txt
pytest>=9.1.1
pytest-cov>=7.1.0
httpx>=0.28.1
# Pinned exactly: an unpinned floor lets a new ruff release expand the default
# ruleset and turn CI red without a code change. Dependabot proposes bumps as
# PRs that must pass CI before merging.
ruff==0.16.3
